openapi: 3.0.3
info:
  title: go-gen-mocks-dep-detector Spec Schema
  version: 0.15.0
  description: Configuration schema for go-gen-mocks-dep-detector

components:
  schemas:
    Spec:
      type: object
      description: Configuration for go-gen-mocks-dep-detector
      properties:
        workDir:
          type: string
          description: Working directory for mock detection (optional)
