name: go-lint-tags
kind: mcp-server
profile: test-runner
version: 0.15.0
description: Verify test files have proper build tags (unit, integration, e2e)

openapi:
  specPath: ./spec.openapi.yaml

generate:
  packageName: main
